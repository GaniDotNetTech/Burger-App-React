import React from "react";
import "./DrawerToggle.module.css";

const drawerToggle = props => (
  <div className="DrawerToggle" onClick={props.drawerToggleHandler}>
    <div />
    <div />
    <div />
  </div>
);

export default drawerToggle;
